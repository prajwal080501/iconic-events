import React from 'react';

const GradientDivider = () => {
    return (
        <div className="w-full h-4 bg-gradient-to-r from-red-500 to-transparent"></div>
    );
};

export default GradientDivider;
